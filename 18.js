var binary = function(arr,q){
    // for(i=0;i<arr.length;i++){
        let low=arr[0];
        let high=arr[arr.length];
        let mid=arr[(low+high)/2];
        console.log(low,high,mid)

    // }

}
console.log(binary([1, 2, 3, 5, 6, 7, 10, 11, 14, 15, 17, 19, 20, 22, 23]),6)